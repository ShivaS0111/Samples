package com.example.myapplication.data.persistence

class Converter {
/*
    @TypeConverter
    fun calendarToDatestamp(calendar: Calendar): Long = calendar.timeInMillis

    @TypeConverter
    fun datestampToCalendar(value: Long): Calendar =
        Calendar.getInstance().apply { timeInMillis = value }

    @TypeConverter
    fun stringToDate(@NonNull date: String): Date {
        try {
            return Date(date)
        } catch (e: Exception) {
            return Date()
        }
    }

    @TypeConverter
    fun dateToString(@NonNull date: Date): String = date.toString()
    */
}